"use strict";


row.update({
    steps: row.seq.map(function(val, idx, arr) {
    })
})

row.update({
    steps: row.seq.map(function(val, idx, arr) {
    })
})

row.update({
    steps: row.seq.map(function(val, idx, arr) {
    })
})

row.update({
    steps: row.seq.map(function(val, idx, arr) {
    })
})

TypeError: object is not a function
x = row.each('loop', 2, 2, 'reorder');
0
row.each('beat', 12, null, function() { row.update({tone: 'sine'}) })
1
row.each('beat', 9, null, function() { row.update({tone: 'organ'}) })
2
row.each('beat', 18, null, function() { row.update({tone: 'square'}) })
3
row.each('beat', 34, null, function() { row.update({vals: [[0,0], [5,0], [9,0], [15,0]]}) })
4
